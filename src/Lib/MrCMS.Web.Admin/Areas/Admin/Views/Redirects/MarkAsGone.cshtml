@model MrCMS.Entities.Documents.Web.UrlHistory
@{
    Layout = "~/Areas/Admin/Views/Shared/_LayoutPopup.cshtml";
}

<form asp-controller="Redirects" asp-action="MarkAsGone" asp-route-id="@Model.Id">
    @Html.ReturnToReferrer()
    <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title">
                @await Html.Resource("Mark as gone")
            </h5>
            <button type="submit" class="btn btn-danger">
                @await Html.Resource("Mark as gone")
            </button>
        </div>
        <div class="modal-body">
            @Html.InfoBlock("Warning", string.Format(await Html.Resource("Redirects - Mark as gone", "Are you sure you wish to mark '{0}' as gone?"), Model.UrlSegment), AlertType.danger)
        </div>
    </div>
</form>